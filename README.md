# recon.git
🔎 The most powerful zero-dependency OSINT tool for Kali Linux. Looks up usernames (80+ sites), emails, phone numbers, names, domains (WHOIS/DNS/SSL/subdomains), and IPs (geo/ASN/ports) — all in one pure-Python file. No pip install needed.
